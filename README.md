# Smartsupp eslint config

## How to install

- Install via npm `npm i smartsupp/smartsupp-eslint -D`
- Create file `.eslintrc.js` with the following content:

```javascript
module.exports = {
	extends: 'smartsupp-eslint',
}
```
